node_modules/
dist/
.next/
# Hardhat build output
artifacts/
cache/
# TypeScript incremental build info
*.tsbuildinfo
# One-off secret scratch files (never commit)
DEPOSIT_MNEMONIC.txt
.env
*.env.local
.turbo/
coverage/
# RSA key files — never commit private keys
*.pem
private_key*
public_key*
.vercel
.env*.local
.kiro
PAWASAVE_AUDIT_2026-09_AND_PLAN.md
